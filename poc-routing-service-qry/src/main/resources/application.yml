server:
  servlet:
    context-path: /query/routes
  port: ${PORT:9094}

spring:
  application:
    name: routes_service_query
  cloud:
    stream:
      bindings:
        input:
          destination: bookings-events
          consumer.headerMode: headers
          contentType: application/json
          group: routes-query-group